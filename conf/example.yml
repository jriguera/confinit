logoutput: split
loglevel: debug
datafile: conf/data.yml

env:
    A: B
    c: D
    eeee: "hola mundo"

start:
    cmd: ["pwd"]

finish:
    cmd: ["pwd"]

process:
  - source: cmd
    operations:
      - destination: /tmp/dest
        regex: '.*\.go'
      - destination: /tmp/templates
        regex: '.*\.template'
        template: true
        data:
          hola: adios
        permissions:
          - glob: "*.txt"
            mode: "0600"
            user: jriguera
            group: jriguera
      - destination: /tmp/temp
        regex: '.*\.template'
        default:
          mode:
            file: "0755"
        template: true
        command:
          cmd: ["{{.Destination}}"]
          env:
            MIPI: pepe
        data:
          hola: hola
