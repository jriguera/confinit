logoutput: split
loglevel: debug
datafile: /boot/config/parameters.yml

process:
- source: /boot/config/etc
  operations:
  - destination: /etc
    regex: '.*/default/.*\.template'
  - destination: /etc
    regex: '.*avahi.*'
    condition: '{{if not .Data.avahi}} DELETE {{end}}'
  - destination: /etc
    regex: '.*hostapd.*'
    condition: '{{if not .Data.hostapd}} DELETE {{end}}'
    permissions:
    - mode: "0640"
      glob: '*/hostapd.conf'
  - destination: /etc
    regex: '.*bluetooth.*'
    condition: '{{if not .Data.bluetooth}} DELETE {{end}}'
  - destination: /etc
    regex: '.*wpa_supplicant.*'
    permissions:
    - mode: "0640"
      glob: '*/wpa_supplicant.conf'
  - destination: /etc
    regex: '.*\.template'
  - destination: /etc
    template: false

# Execute thse scripts during boot
- source: /boot/config/scripts/boot
  operations:
  - destination: /tmp/confinit
    template: true
    delextension: false
    default:
      mode:
        file: "0755"
    command:
      cmd: ["{{.Destination}}"]

